# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type UserDTO {
  id: ID!
  email: String!
  name: String!
  username: String!
  role: Role!
  picture: String
  isGoogleUser: Boolean
}

"""User role"""
enum Role {
  USER
  ADMIN
}

type UserEventDTO {
  id: String!
  userId: String!
  sessionId: String
  component: String!
  action: String!
  metadata: JSON
  createdAt: DateTime!
}

"""
The `JSON` scalar type represents JSON values as specified by [ECMA-404](http://www.ecma-international.org/publications/files/ECMA-ST/ECMA-404.pdf).
"""
scalar JSON

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type UserSessionDTO {
  id: ID!
  role: String!
  user: UserDTO!
  jwtId: String
  ipAddress: String
  deviceInfo: String
  socketId: String
  channel: Channel
  status: SessionStatus!
  lastActivity: DateTime!
  loginAt: DateTime!
  terminatedAt: DateTime
  duration: Float
  createdAt: DateTime!
  updatedAt: DateTime!
  currentComponent: String
  lastComponentActivityAt: DateTime
}

"""Session channel"""
enum Channel {
  REST
  SOCKET
  OTHER
}

"""Session status"""
enum SessionStatus {
  ACTIVE
  IDLE
  OFFLINE
  TERMINATED
  EXPIRED
  ERROR
}

type Query {
  allUsers(page: Int! = 1, limit: Int! = 20, search: String): [UserDTO!]!
  userById(id: String!): UserDTO
  userEvents(page: Int! = 1, limit: Int! = 50, userId: String, action: String, component: String): [UserEventDTO!]!
  allSessions(page: Int! = 1, limit: Int! = 20, search: String): [UserSessionDTO!]!
}